<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

class CreateSubscribersTable extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('subscribers', function (Blueprint $table) {
            $table->id();
            $table->string('mailchimp_id')->nullable()->comment('Newsletter subscribe id');
            $table->boolean('status')->default(1)->comment('check, if active or inactive');
            $table->string('email')->unique();
            $table->string('mailchimp_status')->nullable()->comment('Newsletter subscribe status');
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::dropIfExists('subscribers');
    }
}
